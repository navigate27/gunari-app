# Optional: enables Google Places + Geocoding for real location autocomplete.
# Without these, Gunari falls back to manual lat/lng entry.
# Note: these are exposed to the browser (PRD specifies no backend).
# NEXT_PUBLIC_GOOGLE_PLACES_API_KEY=
# NEXT_PUBLIC_GOOGLE_GEOCODING_API_KEY=